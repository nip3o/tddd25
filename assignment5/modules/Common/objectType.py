""" Simple module to obtain the object type used when connecting to the
    name service.

    Each student must change this type to something unique (e.g.,
    object_StudentId) so as to avoid conflicts.
"""

object_type = "objec897sfyhg8yfu6t"
